num_points = 36

# Main
train.num_batches = 1000
train.batch_size = 128

train.vocab_size = 1
train.render_freq = 1000
train.print_freq = 100
train.log_freq = 50
train.savedir = None
train.loaddir = None
train.device = "cpu"

# Game
Circle.num_points = %num_points
Circle.bias = 0

CircleLoss.num_points = %num_points

