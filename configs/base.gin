# Main
train.vocab_size = 256
train.episodes = 15000
train.render_freq = 1000
train.print_freq = 100
train.log_freq = 10
train.savedir = None
train.loaddir = None
train.device = "cpu"

# Game
ISR.batch_size = 128
ISR.num_rounds = 10
ISR.num_targets = 36
ISR.max_bias = 10
ISR.min_bias = 0

