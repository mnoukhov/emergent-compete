include 'configs/gauss-deter-recverlola.gin'

train.num_epochs = 25
train.batch_size = 64
train.Loss = @CircleL1

Gaussian.lr = reinforce-lr~loguniform(1e-4, 1e-2)
Gaussian.hidden_size = reinforce-hid~uniform(16, 64, discrete=True)

ExactLOLARecver.lr = recver-lr~loguniform(1e-4, 1e-2)
ExactLOLARecver.hidden_size = recver-hid~uniform(16, 64, discrete=True)

ExactLOLARecver.order = 0
ExactLOLARecver.sender_lola_lr = sender-lola-lr~loguniform(1e-4, 1e-1)
