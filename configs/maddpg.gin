train.Sender = @MADDPG
train.Recver = @MADDPG

ReplayBuffer.capacity = 1000000

MADDPG.num_agents = 2
MADDPG.lr = 1e-3
MADDPG.gamma = 0.99
MADDPG.tau = 0.01
MADDPG.batch_size = 128
MADDPG.state_dim = 1

MADDPG.target_update_freq = 100
MADDPG.warmup_episodes = 200

# REMOVE LATER
IteratedSenderRecver.max_bias = 0
IteratedSenderRecver.min_bias = 0
